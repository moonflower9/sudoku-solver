[project]
name = "sudoku-solver"
version = "0.1.0"
description = "A simple backtracking Sudoku solver"
readme = "README.md"
requires-python = ">=3.10"
license = {file = "LICENSE.txt"}

[tool.pytest.ini_options]
pythonpath = ["src"]
